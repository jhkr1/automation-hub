# AGENTS.md

## 1. Project Overview

- 프로젝트명: `automation-hub`
- 목적: Python 기반 업무 자동화 프로젝트 모음
- `namuwiki_trend`의 목적: 나무위키 실시간 검색어 순위(1~10위)를 주기적으로 수집하고 활용하는 자동화 프로젝트
- 각 자동화 프로젝트는 독립 패키지로 관리한다.
- 현재 패키지:
  - `namuwiki_trend`: 나무위키 실시간 검색어 순위(Top 10)를 수집하는 패키지
  - `google_finance`

나무위키 실시간 검색어 순위 데이터에서는 `rank`가 핵심이며, 1~10위 순위 정보를 반드시 보존한다.

프로젝트는 `src/`를 사용하지 않는 flat layout을 따른다.

- `src/` 구조로 변경하지 않는다.
- `shared` 패키지는 Rule of Three 이전에 생성하지 않는다.
- 공통 코드가 세 곳 이상에서 실제로 반복될 때만 공통화를 검토한다.
- 각 패키지의 독립성을 유지한다.

### Architecture Rule

- Application Layer가 흐름을 조정하고, Provider Layer가 외부 시스템을 감싸며, Model Layer가 데이터 계약을 보존한다.
- Collector와 Provider는 상위 Application Layer나 LLM을 직접 호출하지 않는다.

### Dependency Injection Rule

- 외부 시스템과 생성기는 생성자 주입을 우선한다.
- 테스트에서는 Fake/Mock 구현을 주입하고 실제 외부 API를 호출하지 않는다.

## 2. Working Principles

### KISS

- 단순하고 이해하기 쉬운 해결책을 우선한다.
- 필요 이상의 추상화, 계층, 설정을 추가하지 않는다.

### YAGNI

- 현재 요구되지 않은 기능은 구현하지 않는다.
- 미래 가능성만을 근거로 확장 지점을 만들지 않는다.

### SOLID

- 모듈과 함수의 책임을 명확히 유지한다.
- 하나의 변경 이유를 가진 코드만 함께 수정한다.
- 구체적인 요구 없이 복잡한 DI나 추상 계층을 도입하지 않는다.

### Small Sprint, Commit, PR

- 하나의 Sprint에서는 하나의 주요 기능만 다룬다.
- 하나의 커밋에는 하나의 논리적 변경만 포함한다.
- PR은 리뷰 가능한 크기로 유지한다.
- 관련 없는 개선을 같은 작업에 포함하지 않는다.

## 3. Working Workflow

### 작업 시작 전 확인사항

코드를 수정하기 전에 다음을 확인한다.

- 현재 작업 디렉터리와 저장소 전체 파일 구조
- `git status`
- 관련 Python 파일과 테스트
- `pyproject.toml`과 관련 문서
- 기존 구현과 요청사항의 차이
- 기존 사용자 변경사항

### Planning Rule

코드를 수정하기 전에 다음을 먼저 제시한다.

- 현재 상태 요약
- 작업 범위
- 수정할 파일
- 영향 범위
- 검증 방법
- 제외하는 범위

계획과 다른 문제가 발견되면 계획을 갱신하거나 사용자에게 보고한다.

### 구현 순서

1. 설계
2. 최소 범위 구현
3. 테스트 추가 또는 수정
4. 관련 문서 업데이트
5. `python scripts/verify.py` 실행
6. `git diff`와 `git status` 재확인
7. 커밋

### Verification Rule

Before completion run:

```bash
python scripts/verify.py
```

세부 검증 명령과 실패 처리는 `scripts/verify.py`가 담당한다.

### 완료 후 확인사항

- 요청된 기능이 실제로 동작하는가
- 테스트와 Ruff가 통과하는가
- 문서가 실제 구현과 일치하는가
- 민감정보가 포함되지 않았는가
- 요청하지 않은 파일이 변경되지 않았는가
- diff가 최소 범위인가

## 4. Coding Rules

### Minimal Diff Rule

가능한 작은 변경을 수행한다.

- 요청된 기능에 필요한 파일만 수정한다.
- 요청되지 않은 formatting 변경을 하지 않는다.
- 대규모 리팩터링과 동작과 무관한 코드 정리를 하지 않는다.
- 기존 사용자 변경사항을 덮어쓰지 않는다.

### Type Hint Rule

- 새로운 공개 함수에는 인자와 반환 Type Hint를 작성한다.
- 가능한 경우 내부 함수와 주요 변수에도 타입을 명시한다.
- `Any`는 필요한 경우에만 사용하고 이유를 설명한다.

### Docstring Rule

- 새로운 공개 함수와 클래스에는 docstring을 작성한다.
- PEP 257 또는 Google Style 중 하나를 일관되게 사용한다.
- 중요한 인자, 반환값, 예외를 설명한다.

### Exception Rule

- `except Exception`을 기본 처리 방식으로 사용하지 않는다.
- 가능한 구체적인 예외를 처리한다.
- 예외를 조용히 무시하지 않는다.
- 복구할 수 없는 예외는 다시 발생시키거나 호출자에게 전달한다.
- 외부 요청, 파일, 파싱, 설정 검증의 실패를 구분한다.
- 예외와 로그에 민감정보를 포함하지 않는다.

### Logging Rule

외부 시스템과 통신하는 코드에는 상황에 맞는 로그를 남긴다.

- `INFO`: 정상적인 주요 작업 흐름
- `WARNING`: 재시도, 누락 데이터, 부분 실패
- `ERROR`: 작업 실패와 복구 불가능한 오류

로그에 API 키, 비밀번호, 토큰, 쿠키, 개인정보, 전체 payload를 기록하지 않는다.

### Dependency Rule

새 라이브러리를 추가하기 전에 다음을 검토한다.

- 기존 의존성으로 해결 가능한가
- 표준 라이브러리로 해결 가능한가
- 프로젝트 전체에 필요한가
- 유지보수와 배포 부담이 합리적인가

새 의존성을 추가할 경우 추가 이유, 기존 방식의 한계, 버전 범위와 영향을 설명한다.

## 5. AI Rules

AI는 실제 구현된 코드와 검증 결과만 근거로 판단한다.

다음 행위를 금지한다.

- 구현되지 않은 기능을 구현된 것처럼 작성
- 구현되지 않은 기능을 README에 먼저 작성
- 존재하지 않는 API URL이나 명세 추측
- CSS Selector 추측
- HTML 구조를 상상하여 작성
- 외부 시스템 동작을 확인 없이 가정
- 승인 없는 리팩터링 또는 구조 변경
- 요청 범위를 벗어난 기능과 의존성 추가

확인하지 못한 내용은 반드시 `확인하지 못함`이라고 기록한다.

나무위키 작업에서는 실제 endpoint, HTTP method, payload, headers, cookies,
`application/octet-stream` 처리 방식, 응답 형식, DOM 구조와 selector를 확인하기 전 구현하지 않는다.

## Evidence Rule

사실을 기술할 때는 다음 우선순위를 따른다.

1. 실제 코드
2. 테스트 결과
3. 명령 실행 결과
4. 공식 문서
5. 사용자가 제공한 자료

위 근거가 없는 내용은 사실처럼 작성하지 않는다.

추론인 경우 `추론`, 가정인 경우 `가정`이라고 명시한다.

## Root Cause Rule

문제가 발생하면 증상을 먼저 수정하지 않는다.

- 먼저 원인을 분석한다.
- 가능하면 근본 원인을 해결한다.
- 임시 우회 방법은 명확히 표시한다.
- timeout, 재시도, fallback 등을 추가하기 전에 발생 원인과 필요성을 확인한다.

## 6. Stop Rule

다음 상황에서는 구현을 멈추고 사용자에게 보고한다.

- 요구사항이 모호함
- API 명세가 없음
- 실제 데이터 또는 HTML 확인이 필요함
- 외부 시스템 동작을 검증할 수 없음
- 구조 변경이 필요함
- 여러 설계가 가능하고 선택에 따라 결과가 달라짐
- 새 의존성 추가가 필요함
- 기존 사용자 변경사항과 충돌함
- 데이터 손실 또는 비가역적 변경 가능성이 있음

중단 시 확인한 사실·중단 이유·사용자 결정을 보고하며 추측으로 우회하지 않는다.

## 7. Testing Rule

- 새로운 기능에는 가능한 테스트를 추가한다.
- 테스트가 없다면 테스트 추가 가능성을 먼저 판단한다.
- 파서와 변환 로직은 fixture 또는 mock으로 테스트한다.
- 외부 Provider와 SDK는 Fake/Mock으로 대체한다.
- 기본 테스트가 외부 네트워크에 의존하지 않도록 한다.
- 빈 응답, 잘못된 응답, timeout과 부분 데이터를 검토한다.
- 테스트를 실행하지 못하면 원인을 보고한다.

실제 테스트가 없을 경우 `no tests ran`을 성공으로 보고하지 않는다.

## 8. Documentation Rule

### README.md

- 실제로 구현되고 실행 가능한 기능만 설명한다.
- 실행할 수 없는 명령을 안내하지 않는다.
- 계획 중인 기능과 구현 완료 기능을 구분한다.

### DEV_LOG.md

Sprint별 구현 내용, 검증 결과, 문제, 해결 과정과 미해결 사항을 기록한다.

### ARCHITECTURE.md

패키지 구조, 기술 선택, 데이터 흐름과 주요 트레이드오프를 기록한다.

### STUDY_NOTE.md

학습한 Python 문법, 라이브러리와 설계 개념을 기록한다.

모든 문서는 실제 코드와 일치해야 한다.

## 9. Git Rule

작업 전후에 `git status`, `git diff --check`, `git diff`를 확인한다.

- 기존 변경사항을 임의로 삭제하지 않는다.
- 관련 없는 파일을 커밋하지 않는다.
- 커밋 전 테스트와 Ruff 결과를 확인한다.
- 사용자가 요청하지 않은 원격 push를 수행하지 않는다.
- `git reset --hard`, `git checkout --`와 광범위한 삭제를 사용하지 않는다.
- 파괴적 작업은 대상과 복구 가능성을 확인하고 승인을 받는다.

## 10. Completion Report Rule

작업 완료 시 다음을 보고한다.

- 변경한 파일
- 구현한 내용
- 테스트 명령과 결과
- Ruff 명령과 결과
- 문서 변경 내용
- 확인하지 못한 사항
- 남은 위험과 후속 작업

완료되지 않은 작업을 완료했다고 표현하지 않는다.
검증하지 않은 결과를 성공으로 보고하지 않는다.

## 11. Scope and Approval

다음 작업은 사용자 요청 또는 명시적 승인이 필요하다.

- 패키지 구조 변경
- `src/` layout 도입
- `shared` 패키지 생성
- 새 외부 서비스 연동
- 새 라이브러리 추가
- 데이터베이스 또는 저장 형식 변경
- 대규모 리팩터링
- 운영 환경 설정 변경
- 원격 저장소 push
- 파괴적 파일 작업

범위를 벗어난 개선 사항은 별도 후속 작업으로 제안한다.
